# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# Terraform
*.tfstate
*.tfstate.*
*.tfplan
.terraform/
.terraform.lock.hcl
crash.log
# Shared and integration envs carry operator-specific values (account ID,
# email) in tfvars; lab env's tfvars is intentionally checked in (port
# mappings, no secrets).
terraform/environments/shared/terraform.tfvars
terraform/environments/integration/terraform.tfvars

# Integration wrapper output dir: rendered inventory, ssh key, admin
# password, terraform plan files. All ephemeral, all per-run, none safe
# to commit.
build/

# Ansible
*.retry
.ansible/
ansible/collections/ansible_collections/
ansible/.ansible/
ansible/facts.d/

# Editors and OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Secrets
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# Build + CI artifacts
artifacts/
coverage.xml

# OMC scratch and Claude Code local settings
.omc/
.claude/
