---
# Role metadata. Kept minimal: this role is not published to Galaxy, so the
# schema-required platforms list is omitted.
galaxy_info:
  role_name: f5_preflight
  author: NexusF5
  description: Collect BIG-IP pre-upgrade health state and assert readiness.
  license: MIT
  min_ansible_version: "2.17"
dependencies: []
