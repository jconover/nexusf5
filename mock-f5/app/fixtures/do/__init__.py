"""Declarative Onboarding (DO) iControl REST GET response fixtures.

Currently houses the `/inspect` endpoint fixture — the one upstream-shipped
brownfield-extraction primitive for system-level config (per ADR 007 prior-art
table). Future fixtures for other DO sub-endpoints land here.
"""
